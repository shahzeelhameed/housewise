import 'package:flutter/material.dart';

class ContactTeamScreen extends StatelessWidget {
  const ContactTeamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("contact Team"),
      ),
    );
  }
}
